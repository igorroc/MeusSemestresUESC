#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    ifstream file("cases.txt");
    string line;

    getline(file, line);

    while(line != "") {
        
    }

    cout << line << endl;
}